## Setup
1. Install Python from python.org
2. python -m venv venv
3. venv\Scripts\activate
   (if blocked: Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned)
4. pip install pandas requests matplotlib